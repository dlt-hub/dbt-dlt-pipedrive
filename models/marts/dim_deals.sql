/* Table: deals */
{{
    config(
        materialized='incremental'
    )
}}
SELECT
    t.id,
    t.creator_user_id,
    t.user_id,
    t.person_id,
    t.org_id,
    t.stage_id,
    t.title,
    t.value,
    t.currency,
    t.add_time,
    t.update_time,
    t.active,
    t.deleted,
    t.status,
    t.last_activity_id,
    t.last_activity_date,
    t.lost_reason,
    t.visible_to,
    t.close_time,
    t.pipeline_id,
    t.lost_time,
    t.products_count,
    t.files_count,
    t.notes_count,
    t.followers_count,
    t.email_messages_count,
    t.activities_count,
    t.done_activities_count,
    t.undone_activities_count,
    t.participants_count,
    t.expected_close_date,
    t.local_lost_date,
    t.local_close_date,
    t.stage_order_nr,
    t.person_name,
    t.org_name,
    t.formatted_value,
    t.weighted_value,
    t.formatted_weighted_value,
    t.weighted_value_currency,
    t.owner_name,
    t.cc_email,
    t.org_hidden,
    t.person_hidden,
    t._dlt_load_id,
    t._dlt_id,
    t.won_time,
    t.first_won_time,
    t.local_won_date,
    t.next_activity_date,
    t.next_activity_id,
    t.next_activity_subject,
    t.next_activity_type,
FROM  {{ ref('stg_deals') }} as t